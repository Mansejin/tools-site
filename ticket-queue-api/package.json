{
  "name": "ticket-queue-api",
  "version": "0.1.0",
  "private": true,
  "description": "FCFS ticket wait-queue API (Redis ZSET) — sized for ~1k concurrent waiters first",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "loadtest": "node scripts/load-test.mjs",
    "smoke": "node scripts/smoke.mjs"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.21.2",
    "ioredis": "^5.6.1",
    "uuid": "^11.1.0"
  }
}
