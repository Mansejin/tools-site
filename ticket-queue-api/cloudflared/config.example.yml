# Generated by login-and-provision-cloudflare-tunnel.sh — do not commit secrets.
# Copy from config.example.yml; credentials.json is written next to this file on NAS.
tunnel: REPLACE_TUNNEL_ID
credentials-file: /etc/cloudflared/credentials.json

ingress:
  - hostname: ticket-queue-api.mansejin.com
    service: http://api:8787
  - service: http_status:404
