# examdata — 관리자용 시험지 코퍼스

사용자가 구조를 분석하지 않습니다.  
우리가 샘플 `.hwpx` 를 여기에 모아 형식을 파악하고, `exam_formats/` 에 프로필을 남깁니다.

```powershell
cd path\to\edu_works\exam-tools
python analyze_exam_structure.py --write-format
```

실제 시험 파일은 git에 커밋하지 않습니다.
