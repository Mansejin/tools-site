# 음성인식 (로컬). 설치: pip install -r requirements.txt
# 모델은 최초 실행 시 자동 다운로드되며 이후 PC에서만 추론 (클라우드 API 아님)
openai-whisper

# numpy 는 whisper/torch 설치 시 함께 들어옵니다 (어/음 음향검출에 사용)
numpy

# 시스템 도구 (pip 아님):
#   - ffmpeg : brew install ffmpeg / apt install ffmpeg   (ffprobe 포함)
